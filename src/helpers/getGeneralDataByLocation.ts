export const getGeneralDataByLocation = (location: string) : [number, string][] => {

    switch (location){
        case "organizations":
            return []
        default:
            return []
    }
}